[BJUT]
username = 25000000
password = change_me
# auto / 1(宿舍) / 2(bjut_wifi) / 3(有线 lgn)
type = auto
# 留空时自动寻找默认物理网卡，例如 eno1 / enp3s0 / wlan0
interface =
# 宿舍网或 Wi-Fi 的 HTTPS 不可用时，是否允许回退到 HTTP 明文传输密码
allow_http_fallback = false
